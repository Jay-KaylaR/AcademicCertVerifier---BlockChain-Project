{
  "name": "eduverify",
  "version": "1.0.0",
  "description": "EduVerify - certificate verification on Ethereum (Hardhat)",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy-local": "npx hardhat run --network localhost scripts/deploy.js",
    "node": "npx hardhat node"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.7.0",
    "hardhat": "^2.14.0"
  },
  "dependencies": {
    "web3": "^1.8.2"
  }
}