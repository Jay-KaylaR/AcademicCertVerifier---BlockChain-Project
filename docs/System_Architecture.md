# System Architecture

- Smart contract (Certificate.sol) deployed on Ethereum (local or testnet).
- Administration keys controlled by issuing institution.
- Client queries contract for certificate data.
